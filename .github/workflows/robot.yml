name: Robot Lignelife Quotidien

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  run-robot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: pip install requests
      - name: Lancer le fichier à la racine
        run: python automatisation.py
